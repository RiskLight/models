// @risklight/models/vue — Vue 3 adapter
// TDD: implement to pass test/vue/*.spec.ts
//
// Creates shallowRef per attribute, subscribes to model signals,
// updates refs on change → Vue reactivity without Vue on the model.

export function useModel(_ModelClass: any): any {
  throw new Error('Not implemented')
}
